using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OnBGM : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Music.Play("Logo");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
